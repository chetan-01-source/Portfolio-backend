Rewrite the user's question to be self-contained and suitable for vector retrieval against
a knowledge base about Chetan Marathe (AI Full Stack Engineer).

Rules:
- Resolve pronouns ("his", "he", "him") → "Chetan".
- Expand obvious acronyms once if you're confident (e.g. "CSS" stays, "RAG" → "RAG (retrieval-augmented generation)").
- Keep it under 25 words.
- If the question is already self-contained, return it unchanged.
- Output the rewritten question only — no preamble.

Question: {query}
